# Resto de un intento anterior con MySQL/MariaDB (pymysql). Ya no se usa:
# el proyecto corre sobre sqlite (desarrollo) / postgresql (cuando haya .env),
# y pymysql ni siquiera está instalado, así que este import rompía el arranque
# de Django por completo.
